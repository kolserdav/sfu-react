//@ts -check

const { watch, series, task } = require('gulp');
const { copyInterfaces } = require('./scripts/create-interfaces-symlink');

task('watch', function () {
  watch('./packages/client/src/package/types/interfaces.ts').on('change', series(copyInterfaces));
});
