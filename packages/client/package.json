{
  "name": "client",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "source": "src/package/Main.tsx",
  "main": "dist/Main.cjs",
  "module": "dist/Main.esm.js",
  "scripts": {
    "start": "react-scripts start",
    "bundle": "microbundle",
    "rmdist": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\"",
    "compile": "npm run rmdist && npm run bundle",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
